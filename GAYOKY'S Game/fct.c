/*#include "fct.h"

void mouseMove(SDL_MouseButtonEvent b,SDL_Surface * b1,SDL_Surface * image1,SDL_Rect positionecran,SDL_Surface * screen)
{
SDL_Event event;
  if((b.x>=550&&b.x<=1125)&&(b.y>=500&&b.y<=555))
  {
    b1=IMG_Load("b1.jpg");
    positionecran.x=550;
    positionecran.y=500;
    SDL_BlitSurface(b1,NULL,image1,&positionecran);
  }
  
  
  
void mousePress(SDL_MouseButtonEvent b)
{
  if(b.button == SDL_BUTTON_LEFT)
  {
    //handle a left-click
  }
  else
  if(b.button == SDL_BUTTON_RIGHT)
  {
  //handle a rghit-click
  }
}*/
